[package]
name = "lsm6ds3trc"
description = "LSM6DS3TR-C driver, no-std, no-alloc, embedded-hal-async"
edition = "2021"
version = "0.1.0"
license = "Apache-2.0"

[features]
default = []
defmt = ["dep:defmt"]

[dependencies]
embedded-hal-async = { version = "1.0" }

bilge = "0.3"

defmt = { version = "1", optional = true }
